Topic:
Object-Oriented Programming

Problem Number:
13

Problem:
class Rectangle(object):
	def __init__(self, length, width):
		self.length = length
		self.width = width

	def area(self):
		area = self.length*self.width
		return area
Call the area method using the Fence object created in Problem 12. What is returned?

Answer:
Fence.area()
72

